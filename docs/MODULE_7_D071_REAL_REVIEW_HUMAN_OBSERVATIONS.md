# Module 7 D071 real-review human observations

## Scope

This record preserves a read-only human visual review of the immutable package
at `outputs/module7_kmeans_local_background_causal_review_d071/` on
2026-07-20.

The package was not regenerated or modified. Segmentation was not rerun, no
parameter was changed or selected, no profile was registered or approved, and
D046 was not used. Sample sufficiency and representativeness were not assessed.
This record makes no final scientific or acceptability conclusion.

## Manifest verification

Verification occurred before visual review.

- `manifest.json` SHA-256:
  `e9af768a03f1e1bbd821f8f594018711b8328c3542adc6f21cebe0f7675d90a8`.
- All 35 manifest-listed artifacts were present, and every recorded byte size
  and SHA-256 matched.
- The actual package contained exactly those 35 listed artifacts plus
  `manifest.json`; there was no unlisted package file.
- Both external source TIFF hashes and both saved area-32 reference hashes
  matched the identities recorded in the manifest.
- The manifest and selection records retained the exact D071 selection,
  authorized-real-review mode, fixed Position 1 then Position 2 order, two
  started and two completed calls, and no retry.
- The two manifest-listed in-package human-observation rows remained blank.

These checks establish package integrity and contract consistency only. They
do not validate the segmentation scientifically.

## Artifacts visually reviewed

Visual observations below were made only from:

- Position 1 `full_field_preview.png` and the three displayed panels in the
  fixed `focused_review.svg` for P1-R4;
- Position 2 `full_field_preview.png` and the three displayed panels in the
  fixed `focused_review.svg` for P2-R1.

No other crop, field, mask array, component table, earlier review package, or
prior human classification was used to form the visual observations.

## Explicit human observations

### Position 1 complete field

- Cyan contours are visually sparse relative to the number of faint or diffuse
  structures visible across the field.
- The contours are concentrated mainly on brighter compact structures or
  bright interiors rather than broadly following the surrounding faint signal.
- No field-wide contour carpet or broad background-spanning support is visible.

### Fixed P1-R4

- The cyan saved-reference contours and yellow candidate contours coincide
  visually on the same three displayed structures, including the two structures
  intersecting the lower crop boundary.
- The raw-selection-only panel shows no magenta added-support mark.
- Faint structures elsewhere in the crop remain without a displayed contour.

### Position 2 complete field

- Cyan contours are again concentrated mainly on brighter compact structures,
  while many faint or diffuse structures remain without contours.
- No field-wide contour carpet or broad background-spanning support is visible.
- No visually obvious new complete-field object can be attributed to the
  candidate from this candidate-only complete-field preview.

### Fixed P2-R1

- The reference and candidate panels show contours on the same three visible
  structures; no new outlined structure is visible in the candidate panel.
- The raw-selection-only panel shows two isolated magenta pixels. Each lies
  inside a bright structure that already has a displayed reference contour.
- Several other faint or diffuse structures in the crop remain without a
  displayed contour.

## Interpretation boundary

The observations above deliberately do not label any support as recovered
cell, cell completion, nonspecific addition or expansion, acceptable joint ROI,
unacceptable bridge, or another biological class. They do not decide whether
the candidate is acceptable, sufficient, representative, preferable, or fit
for a production profile. Those determinations remain outside this review
record.

